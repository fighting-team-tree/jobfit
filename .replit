modules = ["python-3.11", "nodejs-20"]
run = "echo 'Please start backend and frontend separately or use a foreman script.'"
entrypoint = "server/main.py"

[nix]
channel = "stable-24_05"

[env]
PYTHONPATH = "$PYTHONPATH:$REPLIT_SOURCE_DIR"
